apiVersion: v1
kind: Service
metadata:
  name: {{ include "bitcoin-retry-endpoint.fullname" . }}
  labels:
    {{- include "bitcoin-retry-endpoint.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "bitcoin-retry-endpoint.selectorLabels" . | nindent 4 }}
  ports:
    - name: metrics
      port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
