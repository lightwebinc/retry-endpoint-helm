apiVersion: v1
kind: Service
metadata:
  name: {{ include "retry-endpoint.fullname" . }}
  labels:
    {{- include "retry-endpoint.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "retry-endpoint.selectorLabels" . | nindent 4 }}
  ports:
    - name: metrics
      port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
